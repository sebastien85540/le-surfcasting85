<section class="articles articles-pannel" style="height: 74vh">
  <div class="marge" style="margin-top: 100px"></div>
  <div class="column">
    <h4>POSTS</h4>

    <table class="table is-bordered">
      <thead>
        <th>Nom</th>
        <th>Prénom</th>
        <th scope="col" colspan="3">Pseudo</th>
        <th>Email</th>
        <th>Type</th>
        <th>Actions</th>
      </thead>
      {{#each user}}
      <tbody>
        <td>{{name}}</td>
        <td>{{surname}}</td>
        <td colspan="3">{{userName}}</td>
        <td>{{email}}</td>
        <td>{{accountType}}</td>
        <td>
          <a href="/admin/delete/user/{{_id}}">
            <i class="fas fa-trash-alt"></i>
          </a>
          <a href="/admin/edit/{{_id}}">
            <i class="fas fa-pencil-alt"></i>
          </a>
        </td>
      </tbody>
      {{!-- <script>
        function deleteUser() {
          if (confirm("Voulez-vous supprimer ce post ?")) {
            window.location.replace(`/user/delete/{{_id}}`)
          } else {
            windows.location.replace(`/profile`)
          }
        }
      </script> --}}
      {{/each}}
    </table>
  </div>
  <a href="/profile">profile</a>
</section>